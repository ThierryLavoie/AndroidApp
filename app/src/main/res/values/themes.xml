<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.AndroidApp" parent="Theme.Material3.DayNight">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>

        <item name="colorPrimary">@color/playful_blue</item>
        <item name="colorSecondary">@color/energetic_orange</item>
        <item name="colorSurface">@color/surface_kid</item>
        <item name="android:windowBackground">@color/surface_kid</item>

        <!-- Brighten up cards -->
        <item name="colorSurfaceVariant">#FFFFFF</item>
    </style>
</resources>
